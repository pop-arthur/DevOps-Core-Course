apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "myapp.fullname" . }}-env
  labels:
    {{- include "myapp.labels" . | nindent 4 }}
data:
  APP_NAME: "devops-info-service"
  APP_ENV: "dev"
  FEATURE_VISITS: "true"
